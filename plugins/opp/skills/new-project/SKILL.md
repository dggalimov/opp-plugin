---
name: new-project
description: >-
  Онбординг нового проекта, возобновляемый: разворот структуры (приёмные папки, .gitignore,
  .статус.yaml), опрос паспорта, карточка компании (вызывает company-card), правила проекта,
  оформление, финал (гайд пользователя + git/GitHub по согласию). Применять один раз в начале
  проекта; если онбординг прервали — запустить навык снова, он продолжит с прерванного шага.
  Спека: spec/06-f-user-flow.md (frozen), decisions/0025-user-flow-project-git.md.
---

# Навык: Запуск нового проекта (онбординг)

Провести пользователя по семи шагам онбординга: структура → паспорт → карточка компании →
правила → оформление → гайд и git/GitHub → завершение. Прогресс — в `<проект>/.статус.yaml`;
онбординг возобновляемый — прерванный запуск продолжается с прерванного шага, не с начала.

## Шаг 0. Проверь, не продолжение ли это
Работаем в **ТЕКУЩЕЙ папке** (`pwd`) — пользователь сам создал её и открыл в ней сессию, отдельную
родительскую папку не спрашиваем (спека 06-f, правка ЗР-0027). Если в текущей папке уже есть
`.статус.yaml` и `онбординг.завершён: false` при `онбординг.шаг > 0` — онбординг начат раньше и
прерван. Скажи пользователю «продолжаю онбординг с шага N» и перейди сразу к этому шагу ниже, не
повторяя пройденные. Есть `.статус.yaml` с `онбординг.завершён: true`, либо папка `Проектная
память/` уже существует, а `.статус.yaml` нет (проект старой структуры) — это уже существующий
проект OPP, не новый: не разворачивай заново, продолжи/почини онбординг с уместного шага (карточка
компании/правила могут быть не заполнены). Ничего из этого нет — начни с шага 1.

## Шаг 1. Разворот структуры (в текущей папке)
Никакой родительской папки и имени `<Заказчик> — OPP` — разворачиваем ПРЯМО в текущей папке,
её создал и назвал сам пользователь (спека 06-f, спека 08 §8).

**Защита от чужой папки — перед разворотом:**
```bash
pwd && ls -la
```
Покажи содержимое пользователю. Пустая папка или только служебное (`.git`, `.gitignore` и
подобное) → разворачивай без лишних вопросов. Непустая папка с содержимым, не похожим на
заготовку под этот проект (чужие файлы/чужой репозиторий) → явно спроси подтверждение:
«Разворачиваю структуру проекта прямо здесь, в папке `<путь>` — там уже есть [что увидел].
Подтверждаете разворот именно в этой папке?». Только после «да» — деплой. Похоже на уже
развёрнутый проект OPP (см. Шаг 0) — сюда не возвращайся, это не первый разворот.

Спроси кратко, какие каналы данных ожидаются (документы / база данных / интервью) — не знает
пользователь, оставь все три. Разверни:

```bash
"${CLAUDE_PLUGIN_ROOT}/opp" deploy "." --channels "документы,интервью"
```

(`.` — текущая папка, эквивалент `deploy "$(pwd)"`.) Развёртывание идемпотентно и создаёт: приёмные
**«Входные материалы/»** и **«Встречи/»** — сюда пользователь кладёт свои файлы; технический слой
**«Проектная память/Источники/»** — туда пользователь не ходит, это узлы-источники после разбора;
«Проектная память/», «Документы/»; паспорт, правила и оформление по умолчанию; `.gitignore`;
`.статус.yaml`. Повтор не затирает заполненное. Папки «00 — Управление проектом» в структуре
больше нет. → отметь `онбординг.шаг: 1`.

## Шаг 2. Опрос паспорта
Прими и опрос в чате, и документы (`$ARGUMENTS` или брошенные файлы: ТКП, анкета, любые
вводные). Неструктурированный вход разбери сам; **вопросы можно пропускать** — не держи
пользователя на пустых полях. Собери под состав паспорта
(`${CLAUDE_PLUGIN_ROOT}/reference/passport.template.md`): клиент, задача, цель и критерии
успеха, задача выбора, аудируемые блоки, фокус-уровни, глубина и каналы сценария, команды,
системы, объёмные параметры, рамки. Заполни `<проект>/Паспорт проекта.md` (правь файл или
попроси заполнить в чате). → `онбординг.шаг: 2`.

## Шаг 3. Карточка компании
Запусти навык `company-card` на этом же проекте (Skill company-card). Он сам: соберёт публичные
факты, сохранит узел `SRC-COM` в «Проектная память/Источники/» **и выведет карточку сжатым текстом в чат**
пользователю на сверку. Дождись подтверждения или правок пользователя; правки вносятся в
`SRC-COM` (навык company-card это умеет). → `онбординг.шаг: 3`.

## Шаг 4. Правила проекта (активный короткий опрос)
Покажи пользователю `<проект>/Правила проекта.md` — он уже заполнен значениями по умолчанию
(`reference/principles.default.md`). Задай несколько коротких вопросов по ключевым пунктам,
устраивают ли значения по умолчанию:
- **гейт синтеза** — когда считать разведку достаточной, чтобы запускать синтез;
- **статусная лестница** — устраивает ли «гипотеза → подтверждено цифровым следом →
  подтверждено интервью → согласовано заказчиком», или нужна другая;
- **язык и тон** документов (по умолчанию — русский, экспертный).

Правки пользователя вноси прямо в файл (не в память — это настройка, не факт). → `онбординг.шаг: 4`.

## Шаг 5. Оформление
Сообщи: «Оформление документов по умолчанию — корпоративный стиль Первый Бит (уже
активировано). Если нужно оформление под бренд заказчика — отдельный навык `theme-from-brand`
(понадобятся логотип, сайт заказчика или примеры документов)». Сам навык `theme-from-brand`
не вызывай — это решение и отдельный запуск пользователя. → `онбординг.шаг: 5`.

## Шаг 6. Финал: гайд и git/GitHub
Скопируй гайд пользователя из `${CLAUDE_PLUGIN_ROOT}/reference/guide/` в корень папки проекта
(все файлы, без изменений) — это готовый asset продукта, «Руководство пользователя».

Предложи создать репозиторий: «Хотите вести проект в git/GitHub — для командной работы?
Репозиторий будет **строго приватным** (в нём данные заказчика); файлы крупнее 100 МБ GitHub
отклонит — учтите при добавлении тяжёлых записей». По согласию пользователя — **перед коммитом
покажи состав**: `git add -A` заберёт ВСЁ, что лежит в папке, включая любые файлы, которые
пользователь мог туда добавить сам:

```bash
cd "<проект>" && git init && git add -A && git status
```

Покажи пользователю список файлов, которые уйдут в первый коммит (вывод `git status`/`git diff
--cached --name-only`), и попроси подтвердить состав, прежде чем коммитить. Подтвердил (или список
ожидаемый — только развёрнутая структура и материалы проекта) →

```bash
git commit -m "Онбординг проекта"
```

Проверь, установлен ли `gh`:

```bash
command -v gh
```

Есть `gh` — создай приватный репозиторий и опубликуй:

```bash
gh repo create --private --source=. --remote=origin --push
```

Нет `gh` — честно скажи пользователю, что утилита не установлена, и дай команды для ручного
создания: завести пустой **приватный** репозиторий на github.com, затем
`git remote add origin <url> && git push -u origin main`. В репозиторий идёт всё, кроме
секретов — `.gitignore` минимальный (только мусор ОС/инструментов), секретов в проекте нет
вовсе, они хранятся глобально у сотрудника (`~/.config/opp/secrets.env`). → `онбординг.шаг: 6`.

## Шаг 7. Завершение
Отметь в `.статус.yaml`: `онбординг.завершён: true`, `онбординг.шаг: 7`. Сообщи пользователю,
что онбординг завершён, и следующий шаг — положить материалы в «Входные материалы/» (или
приложить в чат) и запустить навык `analyze-materials`.

## Как двигать .статус.yaml
Правь файл через PyYAML, не текстом руками (не сломать структуру):

```bash
"${CLAUDE_PLUGIN_ROOT}/.venv/bin/python" - <<'PY'
import yaml
p = "<проект>/.статус.yaml"
d = yaml.safe_load(open(p, encoding="utf-8")) or {}
d.setdefault("онбординг", {})["шаг"] = <N>
# на шаге 7 дополнительно:
# d["онбординг"]["завершён"] = True
yaml.safe_dump(d, open(p, "w", encoding="utf-8"), allow_unicode=True, sort_keys=False)
PY
```

## Контракт
Вход: вводные (чат + документы), текущая папка проекта (пользователь сам создал её и открыл
в ней сессию — деплой идёт в неё, `deploy "."`, не в новую вложенную). Выход: развёрнутая
рабочая папка с заполненным паспортом, карточкой компании (`SRC-COM`), правилами и оформлением,
гайдом пользователя в корне; `.статус.yaml` с `онбординг.завершён: true`. Возобновляемый:
прерванный онбординг продолжается с шага, записанного в `.статус.yaml`, а не с начала; непустая
чужая папка и уже существующий проект OPP защищены явной проверкой перед разворотом (Шаг 0–1).
Git проекта — по согласию пользователя, состав первого коммита показывается на подтверждение
до `git commit` (Шаг 6), репозиторий строго приватный (ЗР-0025). Скилы и инструменты не
копируются в проект — резолвятся из продукта.
