"""Контролёр целостности проектной памяти.

Итерация 0 — заготовка: читает память «в модель» (а не разбором по шаблонам), гарантирует, что
пустая память проходит проверку, и ловит запрещённые «устаревшие шапки» в документах. Содержательные
правила (доказуемость, двусторонние связи, якоря, ОБЭ и т.п.) наполняются вместе со схемой в
Итерации 4.
"""

from .violation import Violation
from .structure import check_structure
from .run import lint_workspace

__all__ = ["Violation", "check_structure", "lint_workspace"]
