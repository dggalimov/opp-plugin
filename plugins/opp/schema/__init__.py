"""Контракт таблиц фактов (схема памяти): модель + линтер.

`fact-tables.yaml` — единый источник (reg-facttables). `model.py` читает его в структуру,
`lint.py` проверяет, что контракт корректен. Замкнутость заполненной памяти — отдельно (cap-t-closure).
"""
